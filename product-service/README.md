# product-service

**Files to edit for business logic:**
- `entity/ProductEntity.java` — Add fields (SKU, tags, variants, etc.)
- `dto/ProductDto.java` — Add/modify request/response fields
- `service/impl/ProductServiceImpl.java` — Add search, filtering, pagination
- `controller/ProductController.java` — Add endpoints (search, bulk operations)
- `repository/ProductRepository.java` — Add custom query methods
