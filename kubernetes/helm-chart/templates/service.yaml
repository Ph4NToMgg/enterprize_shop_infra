apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise-shop-service.fullname" . }}
  labels:
    {{- include "enterprise-shop-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enterprise-shop-service.selectorLabels" . | nindent 4 }}
